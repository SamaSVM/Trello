package spd.trello.repository;

import org.springframework.stereotype.Repository;
import spd.trello.domain.FileDB;

@Repository
public interface FileDBRepository extends AbstractRepository<FileDB> {
}
